using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Paddles : MonoBehaviour
{
    // max rotation value
    // min rotation value
    // rotationspeed
    private void Update()
    {
        // if the paddle button is pressed & object rotation != max rotation
            // object rotation += rotationspeed * deltaTime;
        // if the paddle button is released & object rotation != min rotation
            // object rotation-= rotationspeed * deltaTime;
    }
}
